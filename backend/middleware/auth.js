import jwt from "jsonwebtoken";
import { User } from "../models/user_model.js";

// Authentication middleware - emptied as requested
export const authenticateToken = () => {};
export const optionalAuth = () => {};
